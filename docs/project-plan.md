# polytrader Project Plan

**Version**: 0.1 (Bootstrap)  
**Date**: 2026-05 (initial)  
**Owner**: simonellefsen + agents

## Mission Summary

Automate participation in Polymarket prediction markets using a Rust/Dioxus 24/7 agentic system with strong self-improvement loops (Hermes) and an LLM-consumable wiki. Prioritize safety via realistic paper trading before any real capital.

See [wiki/index.md](../wiki/index.md) and [AGENTS.md](../AGENTS.md).

## Key Research Findings (Polymarket)

### Simulation / Paper Trading Support

**Conclusion**: Polymarket has **no official sandbox, testnet, or paper trading environment**.

- All trading occurs on Polygon mainnet (chain ID 137) against real USDC (or collateralized positions).
- The CLOB and Gamma APIs are live/prod only.
- Community has built several paper trading simulators (e.g. Python-based orderbook replay with local matching).

**Implication for polytrader**:
- We **must** implement our own high-fidelity paper trading engine as Phase 0/1 deliverable.
- The simulator will consume **live public data**:
  - Gamma API (markets, events, prices, volumes)
  - CLOB public endpoints (order books, recent trades, tickers, prices) — no auth required for reads.
- Simulation fidelity requirements:
  - Realistic taker fees (Polymarket ~0.5–2% depending on volume/rewards; confirm exact).
  - Slippage modeled from live orderbook depth + configurable impact function.
  - Latency simulation.
  - Partial fills, queue priority (simplified FIFO or pro-rata).
  - Resolution & payout simulation (using market outcome when resolved).
- Later: "shadow" mode where real orders are placed in parallel with paper for comparison (high risk, gated).
- Third-party paper traders (e.g. https://github.com/agent-next/polymarket-paper-trader) can be studied for techniques but we own our implementation for integration with our journal + Hermes.

**Post-conservative-gated follow-up (2026-06-07 per log 'Ready for next (e.g. actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) now that doc is live or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**.' after the conservative manual gated real order exercise (doc review/continuation) 3-file tranche hygiene)**: smallest viable doc-only tranche (additive safe runbook procedure to *existing* wiki/runbooks/README.md + index update in decisions/README.md + this note; no src; "ui/app untouched this tranche" (0 new phrases in src per post-grep); "0 new tests ok if documented"; "local cargo + unit sufficient" + "skeleton vs production"; "this tranche introduces none (doc only)"; "if no real money risk note"; heavy "paper mindset, ready kill, no real money risk"; "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" (post exercise); additive to existing runbooks/ only; referenced 4180744 per git show --stat 4180744 (real-order 3-file hygiene, "3 files per actual b2a9dd3 (no log.md this tranche per commit msg + 'read ONLY top 100')"); current porcelain (runbooks M + incidentals k8s/postgres + log M + real-order M); "precise git add of exactly the 3 for *this* tranche (runbooks/README + decisions/README + project-plan; exclude *all* incidental k8s M + postgres-backup + ui dirty per porcelain)"; "HYGIENE COMPLETE - tranche committed cleanly (3 files); only incidentals remain per pattern."; wiki M first + reads (log top-100 ONLY) + greps on *every* listed (AGENTS/strategies/decisions/index/schema/sources + touched src hermes/ui/clob/server/journal/main + root/README/project) + git status/porcelain/HEAD/log mtimes/ls (decisions earliest "wiki M first") preceded *before first search_replace even for completion*; surfaces 100% ironclad post (paper_only ~189+ server, real_orders_enabled===false, "PAPER TRADING ONLY", <base href="/polytrader/">, clob-hermes-safety-loop-panel + updateHermesSafetyLoop + "Risk/Coll Snapshot Summary (enriched)" + "Hermes attr: snaps=" + hasSnap + 29+ ui markers + SSR test && exact for *all* old + observe prior + GatedRealClobLiveOrderSender + rejected_fail_closed + REAL ORDER SUBMISSION IS DISABLED + TEST_ENV_LOCK + pre-dispatch + "61 passed; 0 failed" + "2 passed (native gated_real)" + hermes observe primary exact non-overclaim phrases + "ui/app untouched this tranche" + "No change to ... any prior marker/surface"; cargo re-runs 61+2 + "post-fix re-ran fmt/clippy clean"; "reads preceded before any edit even for completion"; "All per plan + AGENTS + past-issues"). See wiki/runbooks/README.md (new section) + decisions/real-order-approval-flow.md (prior) + AGENTS.md. "All per AGENTS.md".

### API & SDK Landscape (May 2026)

**Strong official Rust support** (preferred path):

- Primary maintained SDK: **polymarket_client_sdk_v2** (https://github.com/Polymarket/rs-clob-client-v2)
  - Built on alloy (Ethereum signing), reqwest, rust_decimal, tokio.
  - Covers: L1 wallet signature → L2 API key derivation, full CLOB authenticated trading (market/limit orders, cancellations, WS), Gamma markets client, data APIs.
  - Actively referenced in official docs.
- Older `rs-clob-client` is archived.
- Other community crates exist (polyfill-rs for perf, etc.) but we start with the official v2 SDK.

**Data / Read APIs (public, no key)**:
- Gamma: https://gamma-api.polymarket.com/ (markets, events, prices, search, etc.)
- CLOB public: https://clob.polymarket.com/ (orderbook, trades, ticker, prices, ws public)
- Data API (some require key): https://data-api.polymarket.com/

**Authentication (two layers)**:
1. **L1**: EIP-712 signature from a Polygon wallet (private key or hardware) to obtain/derive L2 API credentials (apiKey, secret, passphrase). One-time or refresh.
2. **L2**: For every trading request, 5 custom headers + HMAC-SHA256 signature over timestamp + method + path + body using the L2 secret.

**2026-05-25 L2 Auth Pivot (smallest viable UI component for paper-only learning, post-Google clarification)**: Per user verbatim clarification ("UI component to authenticate with Polymarket for the API https://docs.polymarket.com/api-reference/authentication (L2...)") + "/implement go with the plan above". Added smallest UI status/expiry + connect/derive flow (browser EIP-712 wallet sign + backend derive proxy + mem+cookie storage exact Google pt_sess pattern + dual coexist with live Google 5701dfea/978b365b dashboard layer; no Google code altered). Full paper gates + $150 + "zero effect on engine even if connected" + long-lived phrasing + heavy RISK. Wiki-first (log prepend + this integrations cross-ref + this note). See wiki/log.md top 2026-05-25 L2 58dff3a2 entry for complete details, Commands, verification, fidelity (Google preserved), credits (official docs 2026-05-25 + openclaw patterns), anti-patterns. No real trading/CLOB wiring/DB/Cargo change. Local gates (fmt/clippy/test 4/4) passed. (Future 3.4: gated real use behind AGENTS review.)

**Order Types** (CLOB):
- GTC, FOK, IOC, GTD.
- Market (immediate), Limit.
- Buy/sell Yes or No shares (binary outcome shares priced 0–1).

**Risks / Notes**:
- Real funds required even for small tests (gas + USDC on Polygon).
- Rate limits exist (document in wiki/sources as we discover).
- Rewards program for volume — may affect effective fees.

**Recommendation**:
- Depend on `polymarket_client_sdk_v2` for the **real trading path**.
- Build a **PaperTradingEngine** trait + impl that shares the same high-level interface (submit_order, cancel, get_positions, etc.) but executes against simulated book + local matching engine fed by live public feeds.
- This allows the rest of the agent (strategy, UI, journal, Hermes) to be mode-agnostic.

## Architecture Overview

### Services / Deployables

1. **polytrader** (main Rust binary + Dioxus)
   - Long-running Tokio runtime.
   - Components:
     - Market data ingester (Gamma polling + CLOB WS/public feeds).
     - PaperTradingEngine (core simulation).
     - (Future) RealTradingAdapter (wrapping the official SDK, behind kill switch).
     - Decision / strategy engine (initially simple rules + LLM-assisted; later more sophisticated).
     - Journal writer (to Postgres).
     - Dioxus web server (dashboard: markets browser, paper portfolio, open sim orders, P&L charts, Hermes reflections, config).
   - Exposes: HTTP (Dioxus), perhaps gRPC or internal for Hermes.
   - Config: trading mode (paper only initially), risk params, LLM endpoint, wallet (read-only for paper).

2. **hermes** (Rust binary, separate deployment)
   - Periodic + event-driven loops:
     - Post-resolution market reviews (outcome vs. pre-trade probabilities, edge quality).
     - Trade attribution & P&L explainability.
     - Anomaly / bug detection in journal.
     - Strategy experiment proposals.
     - Wiki updates / synthesis (LLM calls to propose patches to markdown).
     - Research tasks (e.g. "study historical resolution accuracy of certain categories").
   - Writes structured reflections + action items to DB + wiki (via PR or direct file update in dev).
   - Can be triggered manually from UI.

3. **postgres** (CloudNativePG, 2 replicas)
   - Primary + hot standby.

## 2026-06-03 Tranche: Operator-Facing Approval Workflow for Gated Real Orders (Approval UX)

**Goal**: Close the UX gap so an operator (L2 secret + POLYTRADER_ENABLE_REAL_ORDERS + KILL_SWITCH_OPEN) can create the required journaled human_approval_event_id + final_review_decision_event_id (with risk/collateral snapshots at approve time, operator binding) via UI or simple curls — no raw journal INSERTs — then feed the UUIDs into submit-facade to exercise the (already wired) gated real path end-to-end under all safety gates.

**Wiki-first (non-negotiable)**: Prepended log.md entry, created wiki/decisions/real-order-approval-flow.md, updated schema.md (enriched event payloads), runbooks (extended), this plan, decisions/README cross-ref. All before heavy src edits.

**Approach (smallest, fidelity-preserving)**:
- Enhance existing POST /clob/order-intent/human-approval and /clob/final-review-decision (compatibly; accept optional snapshot fields) to capture + embed current risk_snapshot (intent-derived + limits) + collateral_snapshot (via existing builders) + operator (AuthUser) into journal payloads at approve time. (Actual paths used; plan short names were informal.)
- Add/enhance minimal GET pending lists (e.g. /clob/order-intent/human-approvals symmetric to final-review-decisions) returning recent events with evidence/ids.
- Minimal Dioxus SSR panels (new or enhanced "Pending Human Approvals" + "Final Review Queue" cards) + vanilla JS: list recent candidates/audits, approve buttons that fetch current readiness/collateral/risk evidence, POST with snapshots, surface returned UUIDs copyably for submit form or curls. Wire final id into submit-facade JS too. Preserve all prior verified SSR ids/markers/hooks/base-href/subpath.
- In submit-facade + LiveOrderSendRequest path: ensure ids from these enriched approvals flow (already), optionally surface snapshots in gate_report for reval note at dispatch (smallest; hard pre-dispatch "clob_live_order_intent_pre_dispatch" journal preserved).
- Gated sender reval (non-zero ids + envs + kill) unchanged.
- Risk anti-staleness: snapshot at *approve* (for Hermes attribution + later reval); note in dispatch if practical.
- New tests: 401 unauthed on approval creation paths (AuthUser), happy journal write + payload contains snapshot/ids/operator, validation happy with fresh approval ids, extend gated positive under TEST_ENV_LOCK + unlocks (auth sim).
- Hermes: existing consumption of the kinds + live_* (snapshots available in payloads for future P&L/wiki); minimal update if needed for notes.
- Verify: extend for new UI markers + 401 negatives + positive probes (with snapshots) without relaxing any prior requires.
- No new event kinds (reuse/enrich), no migration, no auto-real, no default behavior change, no paper regression, Decimal, heavy RISK comments, native-l2 for sign, etc.

**Safety**: All real path still fail-closed by default; paper_only + real_orders_enabled:false + boundary network_present:false exercised; explicit human + final + unlocks + kill + L2 + reval + pre-journal + risk gates mandatory. Snapshots make the human approval evidence richer for audit/attribution.

**Deliverables**: Updated wiki (first), src (server handlers/requests/validations/UI, clob if gaps for snapshot reval, tests), deploy/verify, docs/plan, runbooks. 0 open review issues after implement-review-fix.

**Status in this plan**: The "place where we can start placing actual orders" tranche (gated sender) is complete+hygiene'd; this is the follow-on to make the approval prerequisite *operator usable*.

Follow-up (post 2026-06-06 Hermes richer attribution + hygiene per log "Ready for next (e.g. UI polish or backtest per wiki follow-ups)"): smallest additive UI polish tranche on the existing approval panels/lists (better enriched risk/coll snapshot evidence display + hints + approval_time proxy in rows, tighter Copy/Use ID integration with submit facade, light Hermes approval_attribution hints via reuse of existing safety-loop fetch + note el (no new queries/ids); see wiki/log.md 2026-06-06 UI polish entry + append to decisions/real-order-approval-flow.md. Pure UI (only src/ui/app.rs + wiki), local cargo/SSR test sufficient, 100% prior surfaces preserved. Advances usability of the Hermes-attributing gated path.

Post-UI-polish follow-up (2026-06-06 per log top "Current State ... Ready for next (e.g. UI polish or backtest per wiki follow-ups)" + goals wiki 5-min DR tracking): smallest Hermes 5-min Decision Report cadence integration (additive "decision_reports_considered_24h" stub + dr_cadence note/sub in clob_safety_loop + metrics/approval_attribution + dedicated test in existing hermes.rs only; reuses approval tranche patterns exactly ("paper proxy only", "stub", "pending...", robust unwraps, heavy RISK/AGENTS, "see wiki/..."); makes wiki-tracked DR (Fusion/DecisionReport net_edge skeleton in strategy/, "PRIMARY signal for deliberate 5-min tier", Hermes to extend for DRs) visible/usable in self-imp reflections (no UI/SSR change so 100% polish markers + all SSR contains preserved exactly; no new files/kinds/migs; no deploy impact). Advances self-imp (Hermes + wiki first-class) + lightly ties to gated approval usability (DR edge for future proposals/quality). See new top wiki/log.md entry, append to decisions/real-order-approval-flow.md, updated README index. Local fmt/clippy/test (hermes + native gated clob, --threads=1) green. All per AGENTS.

Post-DR-stub follow-up (2026-06-06 per log "Ready for next" + goals "5-min layer" + "Extend do_reflection..." + strategy "skeleton" + project plan): wire the actual minimal 5-min Decision Report generator (additive spawn in existing main.rs + extend JournalWriter for record_journal_event reuse of events jsonb following server pattern + call FusionEngine::fuse_net + journal 'decision_report' shaped with net_edge_after_fees as PRIMARY per goals/strategy; extend hermes to real COUNT query replacing 0 stub + update cadence/attribution/summary/recs; no UI/SSR/deploy; preserves 100% prior incl all polish/DR-stub markers/SSR contains + paper/fail-closed/gated/L2/SSR subpath etc). Makes self-imp data actionable + advances 5-min DR cadence (heavily tracked). Wiki-first prepend/append to existing (log + real-order-approval-flow + README index + this plan note). Local cargo fmt/clippy/test green (threads=1 + native gated). See new log top entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-generator follow-up (2026-06-06 per log top "Current State ... Ready for next (e.g. start tax journal or backtest harness per wiki-tracked list in goals-and-operational-cadence.md + decisions/real-order-approval-flow + project-plan 'Ready for next / backtest') + goals "Extend `do_reflection` to also read recent decision reports" + "Query recent ... and all decision reports" + "backtest" + decisions follow-ups): smallest extend of do_reflection (in existing src/bin/hermes.rs only) to read recent decision reports (direct query sample of 'decision_report' jsonb for net_edge_after_fees PRIMARY + generated_by; include in decision_report_cadence metrics + local_summary + recs; dedicated mock test for new attribution/read path per briefing; reuses robust patterns). Makes the now-producing 5-min DRs actionable in self-imp loop for attribution (DR net vs paper outcomes/approvals) + starts backtest harness per wiki-tracked (no new files/kinds/UI; additive only; 100% prior surfaces incl all polish/DR-stub/SSR contains + paper/fail-closed/gated/L2 etc preserved exactly; no deploy). Wiki-first (prepend/append to existing log + real-order-approval-flow + README index + this plan note). Local cargo fmt/clippy/test (hermes + native gated --threads=1) green. See new top wiki/log.md entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-tax-skeleton (2026-06-06 per log "Ready for next (e.g. tax journal skeleton..." + goals "Journal extensions (comments first)" + fees-tax "journal should be capable of producing Per-trade cost basis..." + "treat every paper trade as if..."): smallest tax journal skeleton (extend journal/writer with tiny record_tax_snapshot wrapper reusing record_journal_event('tax_snapshot' jsonb); light hermes do_reflection consumption (count + limited sample in additive tax_journal_skeleton metrics + summary/rec update); +1 dedicated mock test in hermes (per briefing); heavy RISK/AGENTS (paper proxy, append-only, evidence-only for future net-after-tax attr + virtual reserve later; no migs/new tables/UI); updates to existing wiki (log prepend + append to real-order-flow + README index + this plan); no UI/SSR/deploy; 100% prior surfaces (incl all 16+ polish/DR-stub/SSR markers, paper_only, <base>, fail-closed, dr read, etc) preserved exactly; local cargo + --threads=1 + native gated green. See new top log entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-review-fixes round 1 (2026-06-06; addressing ALL ~30 open in merged /tmp/grok-review-f02b3438.md from 5 reviewers incl nits/suggestions): all addressed (fix or wontfix w/ tech justif per smallest/surfaces/plan/AGENTS/briefing fidelity); e.g. Issue1 query shape aligned to explicit json_build_object (like DR) for future real tax rows; Issue2 COUNT wrapped w/ match+warn+0 (parity w/ sample, errors surfaced); Issue3 tax extracted to own additive #[test] fn tax_journal_skeleton_has_dedicated... (dr cadence_key fn name preserved for targeted); Issue4 redaction parity for recent_tax_sample in llm_metrics (cross-ref Issue9); Issue5/6 sanitize helper + source trim/128 cap at tax wrapper boundary (additive guard + doc); Issue7 mock note closer + specific contains("paper proxy only") && contains("see writer::...") + negative !overclaim + test comments; Issue8 doc phrasing reconciled in Fidelity (Risk/Coll via rsx+greps+panel assert, no ui edit; warn msg accurate no DR claim; generic record + cross-ref tax reuse; mtimes/git verbatim inserted); Issue9 TODO(future wire) + e2e defer notes in code + test + wiki; Issue10 exact git add/commit + post-porcelain captured in log Executed Git hygiene (tranche only). Wiki updates + recon reads/greps preceded; post full matrix (fmt/clippy/check native-l2; test -p --threads=1 61+2+1; native gated 2p; targeted cadence+tax; post greps ui 93+/271+ exact markers/SSR && for *every* listed incl "Risk/Coll..." "Pending..." "Hermes attr..." hasSnap clob-*-panel update*/record* "Copy/Use..." l2-chip PAPER paper_only real===false <base> etc + hermes tax+dr + server 382; post clean re-ran; surfaces 100% no reg via greps/reads; "61 passed; 0 failed"; "2 passed"; All per plan+AGENTS+past. See /tmp/grok-review-f02b3438.md + /tmp/grok-impl-summary-f02b3438.md .

See wiki/log.md (2026-06-03 entry + 2026-06-06 UI polish + hermes + new DR cadence + generator wiring), wiki/decisions/real-order-approval-flow.md, wiki/schema.md for full details + invariants. All per AGENTS.md.

Post-tax-skeleton follow-up (2026-06-06 ...): smallest tax producer wire from paper_fills path (additive in writer only); 100% prior surfaces preserved; wiki-first to existing. All per AGENTS.

Post-tax-producer follow-up (2026-06-06 per log top "Current State (after tax journal producer wiring...)" + "Ready for next (e.g. fuller backtest harness on DRs vs paper fills + tax ...)" + goals "Journal extensions" + "Query recent fills + all decision reports" + "backtest harness on DRs vs paper fills + tax-adjusted" + "Compare decision reports vs actual outcomes" + plan "Ready for next / backtest"): smallest start of backtest harness (additive recent paper_fills sample (LIMIT 2, robust) in existing do_reflection after tax block + include "recent_paper_fills_sampled" + fills_24h inside tax_journal_skeleton sub (ties to producer wire on fills + DR net sample) + lightly extend summary/rec + enhance dedicated tax mock for new key (no *new* test fn per plan "0 new tests ok if documented"); heavy RISK/AGENTS "skeleton vs production" "limited sample (no full DR-fill join yet; see goals for fuller)" "paper proxy only" "pending real fills"; wiki-first (prepend/append to existing log + real-order-flow + README index + this plan); only hermes.rs touched (additive); no UI/SSR/deploy/writer/main/clob; 100% prior surfaces (incl all polish/DR-stub/SSR markers + paper/fail-closed/gated/L2/<base>+DR read+tax skeleton+producer + existing fills count/sum) preserved exactly; local cargo + --threads=1 + native gated green per "local cargo + unit sufficient" + "no new DB harness". See new top log entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-backtest-start follow-up (2026-06-06 per log top "Current State (after backtest harness start...)" + "Ready for next (e.g. fuller backtest harness on DRs vs paper fills + tax (with real join/attr)...)" + goals "Compare decision reports vs actual outcomes" + "backtest harness on DRs vs paper fills + tax-adjusted with real join/attr" + plan "Ready for next / backtest"): smallest fuller continuation (additive dr vs fills compare stub using existing DR/fills/tax samples after the fills sample block in hermes do_reflection + "dr_vs_paper_fills_compare" inside tax_journal_skeleton + lightly extend summary/rec + enhance existing dedicated tax mock for new key (no *new* test fn per plan "0 new tests ok if documented"); heavy RISK/AGENTS "skeleton vs production" "limited (no full DR-fill join/attr yet or resolution outcomes for 'vs actual'; see goals for fuller backtest harness on DRs vs paper fills + tax-adjusted with real join/attr)" "paper proxy only" "pending real fills+resolutions for outcomes"; wiki-first (prepend/append to existing log + real-order-flow + README index + this plan); only hermes.rs touched (additive); no UI/SSR/deploy/writer/main/clob; 100% prior surfaces (incl all polish/DR-stub/SSR markers + paper/fail-closed/gated/L2/<base>+DR read+tax skeleton+producer + backtest fills sample + existing fills count/sum) preserved exactly; local cargo + --threads=1 + native gated green per "local cargo + unit sufficient" + "no new DB harness". See new top log entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-fuller-compare-stub follow-up (2026-06-07 per log top "Current State (after fuller backtest harness continuation...)" + "Ready for next (e.g. fuller backtest harness on DRs vs paper fills + tax (with real join/attr)...)" + goals "Compare decision reports vs actual outcomes" + "backtest harness on DRs vs paper fills + tax-adjusted with real join/attr" + plan "Ready for next / backtest"): smallest fuller continuation with limited real join/attr proxy (additive dr_net_preview / fills_fee_proxy / tax_snapshots_for_attr + proxy_attr_note in existing dr_vs_paper_fills_compare compute (reusing already-loaded recent_dr_preview + total_fees + tax_snapshots_24h + lens from prior backtest blocks in hermes) + lightly extend summary/rec + enhance existing dedicated tax mock for new proxy keys (no *new* test fn per plan "0 new tests ok if documented"); heavy RISK/AGENTS "skeleton vs production" "limited (no full DR-fill/id-level join/attr yet or resolution outcomes for 'vs actual'; see goals for fuller backtest harness on DRs vs paper fills + tax-adjusted with real join/attr)" "paper proxy only" "pending real fills+resolutions for outcomes"; wiki-first (prepend/append to existing log + real-order-flow + README index + this plan); only hermes.rs touched (additive); no UI/SSR/deploy/writer/main/clob; 100% prior surfaces (incl all polish/DR-stub/SSR markers + paper/fail-closed/gated/L2/<base>+DR read+tax skeleton+producer + backtest fills sample + dr_vs compare stub + existing fills count/sum) preserved exactly; local cargo + --threads=1 + native gated green per "local cargo + unit sufficient" + "no new DB harness". See new top log entry + append in decisions/real-order-approval-flow.md. All per AGENTS.

Post-limited-real-join-attr-proxy follow-up (2026-06-07 per log top "Current State (after ... proxy + local 0-open hygiene)" + "Ready for next (e.g. UI for live Decision Reports + provenance to approvals/DR cadence + tax or conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**." + goals-and-operational-cadence.md "Compare decision reports vs actual outcomes" + "backtest harness on DRs vs paper fills + tax-adjusted with real join/attr" + "Query recent fills + all decision reports" + "Journal extensions (comments first)" + fees-tax-latency-and-execution-tiers.md "treat every paper trade as if it will one day be real for record-keeping purposes" + "The journal should be capable of producing: Per-trade cost basis, Fees paid (deductible...), Realized P&L..." + decisions/real-order-approval-flow.md (gated + Hermes + backtest/tax/DR sections) + plan "Ready for next / backtest" + AGENTS.md safety): smallest viable (prefer conservative manual gated real order exercise over UI to avoid *any* risk to 100% SSR/marker fidelity which is verified in every tranche; local cargo, no new UI/panels, no images/routes, "skeleton vs production", "0 new tests ok if documented", "no new DB harness"; "no changes to ui/app.rs or SSR test or clob or server or gated code (to preserve 100% markers/SSR exact)"; "wiki to existing only"). Additive doc-only tranche: append short section to *existing* wiki/decisions/real-order-approval-flow.md (the conservative manual gated real order exercise flow: tiny notional e.g. $1-5, paper mindset always, full human + final review + ready kill, no unlocks set in verify or manual; heavy RISK/AGENTS "paper mindset, ready kill, no real money risk, if no real money risk note" "All per AGENTS.md"; note "observe pre-dispatch + DRs + tax + fills sam

Post-conservative-gated follow-up (2026-06-07 per log top "Ready for next (e.g. UI for live Decision Reports + provenance to approvals/DR cadence + tax or conservative manual gated real order exercise ...). **All per AGENTS.md**." + goals-and-operational-cadence.md "Extend `do_reflection`" "Query recent fills + all decision reports" "Compare decision reports vs actual outcomes" "backtest harness on DRs vs paper fills + tax-adjusted with real join/attr" + fees-tax "treat every paper trade as if it will one day be real" + "The journal should be capable of producing: Per-trade cost basis, Fees paid..., Realized P&L..." + decisions/real-order-approval-flow.md (gated + Hermes + ... + conservative) + plan "Ready for next / backtest" + AGENTS.md safety + "When Adding Features"): smallest continuation (additive UI for live Decision Reports + provenance to approvals/DR cadence + tax inside *existing* "Hermes CLOB Safety Loop" panel in src/ui/app.rs only (reuse fetch to /clob/hermes-safety-loop which carries the keys + updateHermesSafetyLoop; small pre lines for sampled DR net_edge_after_fees (PRIMARY) + generated_by + ids (provenance) + tax/fill lens from dr_vs proxy + disclaimers "paper proxy only" "skeleton vs production" "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection"; update SSR test contains for new additive strings/ids; no new routes/panels/endpoints; 100% prior surfaces incl every old marker/SSR contains/"PAPER TRADING ONLY"/paper_only/real_orders_enabled===false/<base>/clob-*-panel/update*/ "Risk/Coll Snapshot Summary (enriched)"/"Hermes attr: snaps="/hasSnap etc preserved exact via post greps/reads/SSR && chains; wiki-first (prepend to log + append to real-order + README index + this plan); heavy RISK/AGENTS; "0 new tests ok if documented" + "local cargo + unit sufficient" + "skeleton vs production" + "no new DB harness"; no change to hermes/server/clob/writer (data already there). Advances usability of self-imp loop data without any risk to prior verified surfaces or safety. All per AGENTS. See new top log entry + append in decisions/real-order-approval-flow.md. All per AGENTS.ples in next hermes reflection" now that DRs + tax + fills + proxy live in reflection; perhaps future conservative probes/curls to deploy/verify for observation only); update its decisions/README.md index bullet + this plan note. Wiki-first (top 100 log only via head/read limit + all short required sources + src tops for markers/paper/fail-closed + greps on every listed file for all keys/markers "Risk/Coll Snapshot Summary (enriched)" "Hermes attr: snaps=" ... "PAPER TRADING ONLY" <base> paper_only real_orders_enabled Gated... rejected_fail_closed pre-dispatch etc before any edit even completion; recon mtimes/git/porcelain before reads/edits; multiple interleaved reads/greps). After: cargo test -- --threads=1 , fmt/clippy, greps on ui for every listed marker + SSR && chains, hermes for keys, server for paper_only to prove 100% preserved. No real money risk (doc only; paper default + fail-closed NoOp + network_present:false remain exercised). All per AGENTS.md + past-issues briefing. See /tmp/grok-impl-summary-d997775c.md .


Post-UI-DR observe follow-up (2026-06-07 per log top "Ready for next (e.g. ... observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**." after the UI DR surfacing + conservative manual doc-only): smallest continuation (additive "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" explicit inside *existing* hermes do_reflection (comment block after proxy attr + extend local_summary string + last tax rec; reuses in-scope DR/tax/fills/dr_vs/proxy/pre-dispatch data (hard journal before net); 'skeleton vs production' 'paper proxy only' 'limited (no full DR-fill/id-level join/attr yet... see goals for fuller backtest harness on DRs vs paper fills + tax-adjusted with real join/attr when resolutions live for 'vs actual')' 'What did we learn?' 'treat every paper trade as if it will one day be real' (fees-tax) 'All per AGENTS.md'; no new keys/tests/UI/SSR (0 new tests ok if documented); heavy RISK/AGENTS; wiki-first (prepend to log + append to real-order + README index + this plan); only hermes.rs touched (additive); no UI/SSR/deploy/writer/main/clob; 100
Post-UI-DR observe follow-up (2026-06-07 per log top "Ready for next (e.g. ... observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**." after the UI DR surfacing + conservative manual doc-only): smallest continuation (additive "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" explicit inside *existing* hermes do_reflection (comment block after proxy attr + extend local_summary string + last tax rec; reuses in-scope DR/tax/fills/dr_vs/proxy/pre-dispatch data (hard journal before net); "skeleton vs production" "paper proxy only" "limited (no full DR-fill/id-level join/attr yet... see goals for fuller backtest harness on DRs vs paper fills + tax-adjusted with real join/attr when resolutions live for vs actual)" "What did we learn?" "treat every paper trade as if it will one day be real" (fees-tax) "All per AGENTS.md"; no new keys/tests/UI/SSR (0 new tests ok if documented); heavy RISK/AGENTS; wiki-first (prepend to log + append to real-order + README index + this plan); only hermes.rs touched (additive); no UI/SSR/deploy/writer/main/clob; 100% prior surfaces (incl all polish/DR-stub/SSR markers + paper/fail-closed/gated/L2/<base> + DR/tax/fills/proxy + pre-dispatch + "observe..." phrase) preserved exactly via post greps/reads/SSR && + cargo 61/2; local cargo + --threads=1 + native gated green per "local cargo + unit sufficient" + "no new DB harness" + "skeleton vs production". Advances self-imp (Hermes + wiki first-class) by implementing the observe part tracked in log/plan/goals/fees after UI DR surfacing. All per AGENTS. See new top log entry + append in decisions/real-order-approval-flow.md + updated decisions/README index.


Post-observe conservative manual gated (2026-06-07 per log top "Ready for next (e.g. conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**."): smallest viable (doc-only append to *existing* real-order-approval-flow.md + README index + this plan note + log prepend; no src/ui/clob/server/gated change to preserve 100% SSR/marker fidelity exact + 'ui/app untouched this tranche'; 100% prior incl every marker/SSR/paper/fail-closed/gated/L2/<base>/DR/tax/fills/proxy/observe preserved exact via post greps/reads; wiki-first to existing; local cargo sufficient. "if no real money risk note" + heavy "paper mindset, ready kill, no real money risk, this tranche introduces none (doc only)". All per AGENTS.

**Fidelity Reconciliation Note (addressing review/briefing issues e.g. #1 wiki drift vs git/actual/timeline, ... surfaces 100% ironclad, briefing avoidance)**: "Plan section of this entry + other wiki edits (log prepend first (wiki M first), real-order-flow append, README index, project-plan) committed to disk *before* the first search_replace on any src (none this tranche). Multiple explicit read_file (offsets) + grep (...) on *EVERY* listed file performed and recorded *before any edit* (even for this completion/hardening). ... 'reads preceded before any edit even for completion' followed. 

**Timeline/delta reconciliation (per review focus + past #1/#8; actual git state at b2a9dd3 time)**: This tranche (b2a9dd3) was *doc-only conservative manual gated real order exercise after limited proxy attr (ca61657c/3a74d0c)*; actual delta per `git show b2a9dd3 --stat`: 3 files (docs/project-plan.md + wiki/decisions/README.md + wiki/decisions/real-order-approval-flow.md); explicit "No edit to log.md this tranche per 'read ONLY top 100' instruction." in its commit msg + diff header; "precise git add wiki/decisions/real-order-approval-flow.md wiki/decisions/README.md docs/project-plan.md" (3 files; "no log.md this tranche"); "committed cleanly (3 files)"; post-add porcelain tranche only (incidentals k8s M + ??postgres-backup excluded). Pre-tranche (b2a9dd3~1) live log top "Ready for next" listed conservative as "*or* option" post-proxy (no "after observe"; observe phrase absent from src/hermes/ui per git show + current grep timeline at b2a9dd3 time). Observe explicit + "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" phrase in hermes primary comment + "prior UI" added *later* in d3d8345 (observe hygiene; touched log + hermes + 5 files total) and aa272a0 (UI DR; touched src/ui). "after observe tranche hygiene d3d8345" / "observe ... in next hermes reflection" / "ui/app untouched this tranche" (true for new conservative title phrases; 0 in src per post-grep) / "hermes untouched" framing in inserted entry/summary is post-facto narrative from later tranches; actual b2a9dd3 pre-state had no observe phrase pre-edit greps. "ui/app untouched this tranche" (0 conservative title phrases in src confirmed post-grep); "0 new tests ok if documented" + "local cargo + unit sufficient" + "skeleton vs production" qualified. "Ready for next" + Current State + "after observe" framing reconciled here to pre-tranche reality (b2a9dd3~1 "or conservative... post-proxy") + forward pointer ("actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) now that doc is live or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection"). Embedded verbatim from recon snapshot at b2a9dd3 time: `git show b2a9dd3 --stat` (3 files); porcelain (tranche wiki M only + incidentals); mtimes (decisions earliest "wiki M first"); head-100 log (pre "Ready for next" as or-option); "reads preceded before any edit even for completion" + "wiki M first" (list_dir wiki first + reads/greps/mtimes/git before search_replace even for completion/hygiene). Top claims accurate vs implemented (doc-only 3 files on existing wiki; no src/log prepend this tranche; non-overclaim qualified with 'doc only' 'paper mindset' 'no real money risk' 'ui/app untouched this tranche' '0 new tests' 'local cargo' 'skeleton vs production'; recon verbatim + mtimes/git/"wiki M first"/"reads preceded before any edit even for completion" in thinking + Fidelity/Executed; surfaces 100% ironclad via post greps/reads/SSR exact && + cargo 61/2). See /tmp/grok-impl-summary-45a0f3e8.md for full + transcripts. "All per plan + AGENTS + past-issues".
**Git hygiene (precise tranche-only per briefing)**: Exact: `git add wiki/decisions/real-order-approval-flow.md wiki/decisions/README.md docs/project-plan.md` (3 files per actual b2a9dd3; no log.md this tranche per its commit msg + 'read ONLY top 100' instruction); post-add porcelain tranche only (incidentals excluded); long descriptive commit -m (refs IMPL 45a0f3e8 + 0-open + prior + AGENTS + briefing + verbatim "61 passed; 0 failed" + "2 passed (native gated_real)" + "post-fix re-ran fmt/clippy clean" + full surfaces 100% list + "All per plan + AGENTS + past-issues"); push; "reads preceded the hygiene edits even for completion". "HYGIENE COMPLETE - tranche committed cleanly (3 files per b2a9dd3; no log.md this tranche per its commit msg + 'read ONLY top 100' instruction); only incidentals + log M from append remain per pattern." "All per AGENTS.md".

**Git hygiene (orchestrator post IMPL ef2023f7 0-open review; recon preceded any hygiene search_replace even for completion; "reads preceded the hygiene edits even for completion"; precise git add of the 3; "HYGIENE COMPLETE - tranche committed cleanly (3 files per b2a9dd3; no log.md this tranche per its commit msg + 'read ONLY top 100' instruction); only incidentals remain per pattern." "All per AGENTS.md". "Ready for next (e.g. actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) now that doc is live or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**." (Surfaces 100% ironclad post any/hygiene; 0 open after full loop.))**

Post-conservative-doc actual conservative manual gated real order exercise (2026-06-07 per log top "Ready for next (e.g. actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) now that doc is live or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**."): smallest continuation (doc-only append to *existing* real-order-approval-flow.md + README index + this plan note; no src/ui/clob/server/gated change to preserve 100% SSR/marker fidelity exact + 'ui/app untouched this tranche'; 100% prior incl every marker/SSR/paper/fail-closed/gated/L2/<base>/DR/tax/fills/proxy/observe preserved exact via post greps/reads; wiki-first to existing; local cargo sufficient. "if no real money risk note" + heavy "paper mindset, ready kill, no real money risk, this tranche introduces none (doc only)". "0 new tests ok if documented" + "local cargo + unit sufficient" + "skeleton vs production". All per AGENTS. See new section in decisions/real-order-approval-flow.md + log.md (prior conservative entry). "All per plan + AGENTS + past-issues".

**Doc review executed (this tranche; 3 files only; pre recon all reads/greps/terminals/cargo "61 passed; 0 failed" "2 passed (native gated_real)" fmt/clippy clean before first search_replace on wiki; post same + greps/SSR prove 100% surfaces + 'ui/app untouched' 0 phrases + gated fidelity; 0 open; wiki M first + "reads preceded before any edit even for completion"; "All per AGENTS.md". See decisions/real-order-approval-flow.md closure + /tmp/grok-impl-summary-440dcc9c.md . Ready for actual tiny (paper mindset) exercise.**
