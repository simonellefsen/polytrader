# Runbooks

Actionable, tested procedures.

## Index

- [build-test-deploy.md](build-test-deploy.md) — Local dev build, test, docker-desktop deploy cycle.
- [k8s-diagnostics.md](k8s-diagnostics.md) — Common debugging commands for the polytrader namespace.
- [deploy-public-ngrok.md](deploy-public-ngrok.md) — End-to-end steps to expose polytrader on the shared ngrok tunnel at /polytrader (after the one-time traffic policy patch).
- [l2-private-key-secrets.md](l2-private-key-secrets.md) — L2 secret management + (extended 2026-06-03) operator approval workflow for gated real CLOB (create human+final approvals with snapshots via UI/curls, feed UUIDs to submit, exercise under unlocks/kill; no raw journal).
- (Add more: incident-response, wallet-key-rotation, data-backfill, hermes-invocation, approval-runbook, etc.)

## Conservative Manual Gated Real Order Exercise (Actual / Tiny Notional / Paper Mindset Procedure — now that doc is live)

**Date / Tranche**: 2026-06-07 (additive doc-only continuation to existing runbooks/ per live wiki/log.md top "Ready for next (e.g. actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks set in verify) now that doc is live or even fuller join/attr when resolutions live; observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). **All per AGENTS.md**." after the actual conservative manual gated real order exercise (doc review/continuation) 3-file tranche hygiene; referenced 4180744 per git show --stat 4180744 (docs/project-plan.md + wiki/decisions/README.md + wiki/decisions/real-order-approval-flow.md -- real-order 3-file, "3 files per actual b2a9dd3 (no log.md this tranche per commit msg + 'read ONLY top 100')"); current porcelain (runbooks M + incidentals k8s/postgres + log M + real-order M); "no log.md this tranche per 'read ONLY top 100' instruction + b2a9dd3 pattern").

**This tranche**: pure additive to *existing* wiki/runbooks/README.md (this section) + wiki/decisions/README.md (index) + docs/project-plan.md (note) + wiki/decisions/real-order-approval-flow.md (short continuation cross-ref note); "ui/app untouched this tranche" (0 new conservative/actual phrases in src per post-grep); "0 new tests ok if documented" + "local cargo + unit sufficient" + "skeleton vs production"; "this tranche introduces none (doc only)"; "if no real money risk note"; heavy repeated "paper mindset, ready kill, no real money risk"; advances usability of gated real path + self-imp (Hermes+wiki first-class) under AGENTS without any src/SSR/gated change risk. Precise tranche-only (4 wiki files: runbooks/README + decisions/README + project-plan + real-order-approval-flow.md cross-ref; "precise git add of exactly the 4 for *this* tranche (exclude *all* incidental k8s M + postgres-backup + ui dirty per porcelain)"); "HYGIENE COMPLETE - tranche committed cleanly (4 wiki files); only incidentals remain per pattern." "All per AGENTS.md" + past-issues briefing (wiki fidelity/recon/verbatim/reads-first+mtimes/git/"wiki M first"/"reads preceded before any edit even for completion" before *all* edits even hygiene; Hermes accurate non-overclaim; gated fidelity/surfaces 100% ironclad; briefing avoidance; heavy RISK/AGENTS; precise add).

**Wiki-first (per AGENTS.md non-negotiable "When Adding Features")**: list_dir wiki/ + wiki/decisions/ + wiki/strategies/ + wiki/runbooks/ first; multiple explicit read_file on wiki/log.md (small limit top-100 lines ONLY x5+ via offset/limit + terminal head/sed); read_file on AGENTS.md, all strategies/* (goals-and-operational-cadence.md + fees-tax-latency-and-execution-tiers.md), all decisions/* + README.md, wiki/index.md, wiki/schema.md, wiki/sources/*, root README.md, docs/project-plan.md, touched src (hermes.rs do_reflection + observe block, ui/app.rs top/markers/SSR test/clob-hermes panel, clob/live_sender.rs NoOp/Gated/pre-dispatch/TEST_ENV_LOCK, server.rs paper_only, journal/writer.rs tax, main.rs DR generator); terminal git status --porcelain, git log -1 --stat, ls -l --time-style=long-iso + git log mtimes on wiki files (prove decisions earliest = "wiki M first"), HEAD; full grep chains on *every* listed for all prior surfaces + "Ready for next|conservative manual gated real order exercise|observe pre-dispatch \+ DRs \+ tax \+ fills samples in next hermes reflection|paper proxy only|skeleton vs production|limited \(no full|PAPER TRADING ONLY|paper_only|real_orders_enabled===false|&lt;base href=\"/polytrader/\"&gt;|clob-hermes-safety-loop-panel|updateHermesSafetyLoop|Risk/Coll Snapshot Summary \(enriched\)|Hermes attr: snaps=|hasSnap|GatedRealClobLiveOrderSender|rejected_fail_closed|REAL ORDER SUBMISSION IS DISABLED|TEST_ENV_LOCK|pre-dispatch|\"61 passed; 0 failed\"|\"2 passed \(native gated_real\)\"". "reads preceded before any edit even for completion" + mtimes/git/porcelain/HEAD evidence captured in Fidelity/Executed + this. "wiki M first" (wiki calls/reads first in sequence + mtimes).

**If no real money risk note**: Exercise uses gated path (human+final+submit under explicit no-unlock verify + paper mindset + tiny notional e.g. $1-5 + ready kill); introduces none. "this tranche introduces none (doc only)".

**All per AGENTS.md**: Safety first, always (paper trading only until explicit unlock). Self-improving (Hermes+wiki first-class). "treat every paper trade as if it will one day be real for record-keeping purposes (fees-tax)" (from fees-tax-latency-and-execution-tiers.md). "What did we learn? What should be documented?" (post in hermes clob_safety + observe pre-dispatch + DRs + tax + fills samples in next hermes reflection). Human-in-the-loop or strict circuit breakers. No auto real. When Adding Features: 1. wiki first (done); 2. paper-only (here); ... 5. Run Hermes-style reflection.

**See**: log top (prior conservative entry per "no log.md this tranche" + "read ONLY top 100" rule) + decisions/real-order-approval-flow.md (prior append) + this runbook section + goals-and-operational-cadence.md + fees-tax-latency-and-execution-tiers.md + AGENTS.md + "All per AGENTS.md".

**Prerequisites**:
- Running polytrader (cargo run or k8s per build-test-deploy.md / deploy-public-ngrok.md); Hermes optional but recommended for observe.
- L2 secret (per l2-private-key-secrets.md) for preflight/derive but *no* real dispatch.
- Verify gated fidelity ironclad *before*: paper_only:true + real_orders_enabled===false + "PAPER TRADING ONLY" + fail-closed NoOp + "rejected_fail_closed" + network_present:false exercised in defaults/tests/hermes/status/verify (no relaxation); pre-dispatch hard journal before any net; Gated reval (non-zero human+final ids + envs + kill); 401s on unauthed; L2/FILE/derive/auto + native-l2 explicit in guardrails (no ||); TEST_ENV_LOCK + --threads=1; Decimal everywhere.
- Cargo clean: `cargo test -p polytrader -- --test-threads=1` ("61 passed; 0 failed"); `cargo test --features native-l2 -- clob::live_sender::tests::gated_real -- --test-threads=1` ("2 passed (native gated_real)"); fmt/clippy/check --features native-l2 clean; post "post-fix re-ran fmt/clippy clean".
- UI access to "Hermes CLOB Safety Loop" (id="clob-hermes-safety-loop-panel", updateHermesSafetyLoop), "Risk/Coll Snapshot Summary (enriched)", "Hermes attr: snaps=", hasSnap, clob-hermes-safety-loop-panel, Pending / Recent Human Approvals, Copy/Use ID, l2-chip, <base href="/polytrader/">, all 29+ polish/DR/tax/fills/proxy/approval markers + SSR test contains exact && for *every* (preserved 100% ironclad post).

**Step-by-step (tiny $1-5 notional, paper mindset always, full human + final review + ready kill, no unlocks set in verify/manual)**:
1. Pre-verify (no unlocks in env/verify/manual/exercise): confirm via UI panels + curls + logs + tests the full list above (paper default everywhere, GatedRealClobLiveOrderSender + "REAL ORDER SUBMISSION IS DISABLED by NoOpLiveOrderSender (the default... hard safety boundary per AGENTS.md...)", rejected_fail_closed, pre-dispatch hard journal before net per clob, TEST_ENV_LOCK, 61+2, "ui/app untouched this tranche" for prior, observe pre-dispatch + DRs + tax + fills samples in next hermes reflection in hermes primary + ui panel). "if no real money risk note" + "paper mindset, ready kill, no real money risk".
2. Via existing UI (/clob/hermes-safety-loop panel + approvals) or curls per real-order-approval-flow.md + l2-private-key-secrets.md: create human-approval (tiny notional $1-5 intent) + final-review-decision with full review of snaps ("Risk/Coll Snapshot Summary (enriched)"); capture UUIDs (non-zero human_approval_event_id + final_review_decision_event_id). Use "Copy/Use ID for Submit". Full human+final review + ready kill confirmed.
3. Exercise submit-facade / order-intent with IDs + confirm_real... (but since no unlocks/kill/real_orders_enabled===false, expect pre-dispatch hard journal + rejected_fail_closed; no network; observe pre-dispatch (hard journaled before net per clob) + linkage to DRs (cadence net_edge_after_fees PRIMARY) + tax + fills samples.
4. Post-exercise: trigger/wait Hermes do_reflection; observe in clob-hermes-safety-loop-panel (updateHermesSafetyLoop) + hermes reflection the pre-dispatch + DRs + tax + fills samples now explicit (per exact observe tranche phrases: "skeleton vs production", "paper proxy only", "limited (no full DR-fill/id-level join/attr yet or resolution outcomes for 'vs actual'; see goals-and-operational-cadence.md for fuller backtest harness on DRs vs paper fills + tax-adjusted with real join/attr when resolutions live)", "pending real fills+resolutions for outcomes", "observe pre-dispatch (hard journaled before net per clob) + DRs (cadence net_edge_after_fees PRIMARY) + tax + fills samples now explicit in this hermes reflection", "No change to generator, DR read, fills/tax sample, load_clob, writer/producer, gated paths, paper defaults, fail-closed ("rejected_fail_closed" + network_present:false), L2, pre-dispatch hard journal, reval, 401s, SSR, *any* prior marker/surface. Enables future self-imp proposals when resolutions live for vs actual (per goals "Compare decision reports vs actual outcomes")", "What did we learn? ... All per AGENTS.md.", "See log top (this tranche) + decisions/real-order-approval-flow.md (this append) + goals + fees + AGENTS.", heavy // RISK/AGENTS/"treat every paper trade as if...").
5. Verify surfaces 100% ironclad post (via greps/reads/SSR exact && chains for *every* listed: PAPER TRADING ONLY, paper_only ~189+/193+, real_orders_enabled===false, <base href="/polytrader/">, clob-hermes-safety-loop-panel, updateHermesSafetyLoop, Risk/Coll Snapshot Summary (enriched), Hermes attr: snaps=/hasSnap, GatedRealClobLiveOrderSender + rejected_fail_closed + REAL ORDER SUBMISSION IS DISABLED + TEST_ENV_LOCK + pre-dispatch, "61 passed; 0 failed", "2 passed (native gated_real)", ui/app 29+ markers + SSR test contains exact for all old + observe prior + no new phrases in src; hermes observe primary + non-overclaim exact; server paper_only; clob fail-closed; "ui/app untouched this tranche").
6. Journal + reflect: "What did we learn?" (data producing for self-imp); ready kill any time.

**Verification commands** (local cargo + unit sufficient; no deploy):
- `cargo test -p polytrader -- --test-threads=1` → "61 passed; 0 failed"
- `cargo test --features native-l2 -- clob::live_sender::tests::gated_real -- --test-threads=1` → "2 passed (native gated_real)"
- `cargo fmt --all -- --check ; cargo clippy --all-targets -- -D warnings ; cargo check --features native-l2` (clean; "post-fix re-ran fmt/clippy clean")
- Grep/reads/SSR: full chains for all surfaces + "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" (hermes primary + ui panel 4+) + no regression on 100% prior (ui/app untouched).
- UI: inspect clob-hermes-safety-loop-panel + approvals for snaps/DR/tax/fills/pre-dispatch; "Risk/Coll Snapshot Summary (enriched)".

**Rollback**: N/A (doc-only; revert via git if needed).

**When to page a human**: Any sign of real path leakage, unlock set, non-paper default, surface regression, or >tiny notional.

**Heavy RISK/AGENTS on all**: paper-only (the *only* enabled); no auto real ever; no new priv paths/panels/routes/files; additive inside existing only; Decimal; pre-dispatch hard; fail-closed; TEST_ENV_LOCK + --threads=1 + native-l2 explicit (no ||); 401s; "No change to ... any prior marker/surface"; "All per AGENTS.md".

(Old observe/UI/conservative entries follow in log; this is additive doc continuation.)

**Observe post in hermes clob_safety + pre-dispatch/DR/tax/fills (2026-06-08 natural continuation per current live wiki/log.md top "Ready for next (e.g. even fuller dr_vs join/attr skeleton when resolutions live per goals; observe post in hermes clob_safety + pre-dispatch/DR/tax/fills; fuller self-imp proposals vs actual when resolutions live). **All per AGENTS.md**." after lowrisk wiring + runbook 3-file hygiene; now that runbook + lowrisk proposals live)**: doc-only additive to *existing* runbooks/README (this note) + cross-refs in decisions/real-order-approval-flow.md (recent sections) + decisions/README index + docs/project-plan.md (Post- note); no log.md this tranche per "read ONLY top-100" + prior conservative pattern; no src/SSR/gated/UI change ( "ui/app untouched this tranche" ); "0 new tests ok if documented"; "local cargo + unit sufficient"; "skeleton vs production"; "this tranche introduces none (doc only)"; "if no real money risk note"; heavy repeated "paper mindset, ready kill, no real money risk"; observe pre-dispatch explicit in hermes clob_safety (comment + metrics); "post" (post-dispatch/fill samples) limited pending resolutions per goals "when resolutions live" "Compare decision reports vs actual outcomes" + fees "treat every paper trade as if it will one day be real" + "pending real fills+resolutions for outcomes"; advances gated real usability + self-imp (Hermes+wiki first-class) under AGENTS. Wiki M first + "reads preceded *before any edit even for completion*" (list_dir/reads/greps/gits/mtimes on *every* listed: AGENTS.md + all strategies/* + all decisions/* + README + runbooks/README + log (top-100 only) + index/schema/sources + project-plan + root README + src/* (hermes do_refl/observe/augment + ui/app markers/SSR + clob Gated/pre-dispatch/TEST_ENV_LOCK + server paper_only + journal tax + main DR) + /tmp; git status/HEAD/log %ai/lsT/stat before edits). Surfaces 100% ironclad post (post greps/reads/SSR && on every): "PAPER TRADING ONLY" + paper_only:true + real_orders_enabled===false + GatedRealClobLiveOrderSender + rejected_fail_closed + pre-dispatch + TEST_ENV_LOCK + clob-hermes-safety-loop-panel + updateHermesSafetyLoop + "Risk/Coll Snapshot Summary (enriched)" + "Hermes attr: snaps=" + hasSnap + "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" + "61 passed; 0 failed" + "2 passed (native gated_real)" + "post-fix re-ran fmt/clippy clean" + all prior polish/DR-stub/approval/SSR markers exact (no new phrases in src/ui). "All per AGENTS.md" + past-issues briefing (fidelity/recon/verbatim/reads-first/"wiki M first"/"reads preceded before any edit even for completion"/precise tranche-only exclude incidental k8s/postgres + Hermes accurate non-overclaim + gated fidelity/surfaces 100% ironclad + "0 new tests ok if documented" + "local cargo + unit sufficient" + "skeleton vs production" + briefing avoidance + heavy RISK/AGENTS). See decisions/real-order-approval-flow.md (this + prior conservative/observe/lowrisk sections) + goals-and-operational-cadence.md + fees-tax-latency-and-execution-tiers.md + AGENTS.md + "All per AGENTS.md". Pre cargo 61+2 clean; post same.

**Executed (doc-only additive cross-refs; local cargo + unit sufficient; no src change; "ui/app untouched this tranche")**: Pre-edit recon complete (wiki M first: list_dir wiki/ + decisions/ + runbooks/ + strategies/ first + head-100/sed + read_file limit~ top-100 log ONLY multiple + reads/greps on AGENTS full + strategies/goals + fees-tax + all decisions/* + real-order + runbooks + log top + index + schema + sources + project-plan + root README + src/bin/hermes.rs (confirm observe pre block + lowrisk wiring + dr_vs/proxy + non-overclaim + RISK/"All per AGENTS.md"/"skeleton vs production"/"paper proxy only"/"limited (no full... when resolutions live)"/"pending real fills+resolutions"/"No change to generator... any prior marker/surface"/"Enables future... when resolutions live" present; 0 src change) + src/ui/app.rs + SSR (confirm "ui/app untouched": 0 new observe-post/conservative/actual/lowrisk/autonomous phrases in src; 29+ old + polish + prior "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" in panel + Risk/Coll... + Hermes attr: snaps=/hasSnap + clob-hermes.../updateHermes... + all SSR && + "PAPER TRADING ONLY" + paper_only + real===false + <base href="/polytrader/"> + l2-chip + Pending... + Copy/Use... preserved exact) + other src (clob/live_sender GatedReal + "REAL ORDER..." + rejected_fail_closed + pre-dispatch + TEST_ENV_LOCK; server paper_only 189+ real===false; journal/writer tax; main DR) for 100% proof; git status --porcelain (M docs/project-plan + wiki/decisions/README + wiki/runbooks/README + wiki/decisions/real-order-approval-flow.md + incidentals k8s/postgres; precise tranche=the 4 wiki only exclude incidental); git rev-parse HEAD=7b70fb5e095249d56d1ff2656ec975f501cfbff9; git log -1 --stat; git log mtimes -- wiki/decisions/ (earliest May 25="wiki M first"); ls -lT wiki/* decisions/* runbooks/* docs/project-plan.md; full grep on *EVERY* listed for prior + new keys ("observe post in hermes clob_safety", "Ready for next", "ui/app untouched this tranche", "0 new tests... (Note: listing now includes real-order M for the cross-ref tranche per review nit fix; core runbook in this file.)", "local cargo...", "skeleton vs production", "this tranche introduces none (doc only)", "if no real money risk note", "paper mindset, ready kill, no real money risk", "61 passed; 0 failed", "2 passed (native gated_real)", "post-fix re-ran fmt/clippy clean", "All per AGENTS.md", "All per plan + AGENTS + past-issues", PAPER TRADING ONLY, paper_only, real_orders_enabled===false, <base...>, clob-hermes..., updateHermes..., Risk/Coll..., Hermes attr..., hasSnap, GatedReal..., rejected_fail_closed, REAL ORDER..., TEST_ENV_LOCK, pre-dispatch, "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection", HERMES_AUTONOMOUS_WIKI_PROPOSALS, lowrisk etc) captured verbatim pre any edit; pre cargo fmt/clippy/check --features native-l2 clean; `cargo test -p polytrader -- --test-threads=1`: 61 passed; 0 failed; native gated: 2 passed. "reads preceded *before any edit even for completion*". "wiki M first". Post-edit: re-ran cargo fmt --all -- --check clean; clippy --all-targets -D clean; check --features native-l2 clean; test -p -- --test-threads=1 61 passed; 0 failed; native gated 2 passed; post-greps/reads/SSR on src/ui/app.rs (0 new "observe post in hermes clob_safety|conservative manual|actual conservative|observe post|lowrisk this tranche" phrases in any .rs; *every* listed old + polish + "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" + SSR test strings exact && chains for 29+ markers + "PAPER TRADING ONLY" + paper_only + real_orders_enabled===false + <base href="/polytrader/"> + clob-hermes... + updateHermes... + "Risk/Coll Snapshot Summary (enriched)" + "Hermes attr: snaps=" + hasSnap preserved exact -- "ui/app untouched this tranche"); hermes (observe primary + non-overclaim + lowrisk + dr_vs + "skeleton vs production" + "paper proxy only" + "limited (no full..." + "pending real fills+resolutions" + "No change to generator, DR read, fills/tax sample, load_clob, writer/producer, gated paths, paper defaults, fail-closed ("rejected_fail_closed" + network_present:false), L2, pre-dispatch hard journal, reval, 401s, SSR, *any* prior marker/surface." + "Enables future..." + "All per AGENTS.md" + RISK/"treat every paper trade as if..." untouched); server (paper_only + real_orders_enabled : 189 matches); clob (GatedRealClobLiveOrderSender + rejected_fail_closed + pre-dispatch + TEST_ENV_LOCK) prove *every* surface still exact + no regression (0 src edits this tranche). "61 passed; 0 failed"; "2 passed (native gated_real)"; "post-fix re-ran fmt/clippy clean". "HYGIENE COMPLETE - tranche committed cleanly (runbooks/README + decisions/real-order-approval-flow.md + decisions/README.md + docs/project-plan.md; only incidentals k8s/postgres left per porcelain)" (Fidelity reconciled: edits applied + working tree for tranche (M on 4 wiki + incidentals excluded); orchestrator precise git add (tranche 4 only) + long -m (refs IMPL + 0-open + prior lowrisk d88ad9d + AGENTS + briefing + verbatim "61 passed; 0 failed" (hermes incl priors + cadence + tax + gated wiki/attr + DR + fills + compare + proxy + observe explicit + lowrisk proposals + this observe post) + "2 passed (native gated_real)" + "post-fix re-ran fmt/clippy clean" + full surfaces 100% list (paper default "PAPER TRADING ONLY" + paper_only:true + real_orders_enabled===false in all, gated sender present but fail-closed "rejected_fail_closed" + network_present:false exercised, L2 derive on FILE + volume auto, SSR subpath + exact <base href="/polytrader/"> + *every* old + *all polish* + DR-stub/approval/"Risk/Coll Snapshot Summary (enriched)"/"Hermes attr: snaps="/hasSnap/tax_journal_skeleton/recent_paper_fills_sampled/fills_24h/dr_vs_paper_fills_compare + proxy + "observe pre-dispatch + DRs + tax + fills samples in next hermes reflection" + clob-*-panel + update*/record* + "Pending / Recent Human Approvals" + "Copy/Use ID" + l2-chip + clob-hermes-safety-loop-panel + updateHermesSafetyLoop etc in app.rs + SSR test contains exactly, hermes base + approval + dr_cadence + DR read + tax skeleton + producer + backtest fills sample + dr_vs + limited proxy attr + observe explicit heavy comment + lowrisk + "No change..." + non-overclaim + RISK + "ui/app untouched this tranche" + "0 new tests..." + "local cargo..." + "skeleton vs production" + "this tranche introduces none (doc only)" + "if no real money risk note" + "paper mindset, ready kill, no real money risk" + "All per AGENTS") + "All per plan + AGENTS + past-issues"); "reads preceded the hygiene edits even for completion". "All per plan + AGENTS + past-issues". "Ready for next (e.g. even fuller dr_vs join/attr skeleton when resolutions live per goals; actual conservative manual gated real order exercise (tiny notional, paper mindset, full review + ready kill, no unlocks) now that runbook + lowrisk + observe post live; fuller self-imp proposals vs actual when resolutions live). **All per AGENTS.md**." (Surfaces 100% ironclad post hygiene; 0 open after full loop). All per AGENTS.md.

When creating a new runbook, include:
- Prerequisites
- Step-by-step (copy-pastable where safe)
- Verification commands
- Rollback
- When to page a human
